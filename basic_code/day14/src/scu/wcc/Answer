1.一个引用变量可以指向不同的对象类型叫多态；条件是：继承/实现关系、父类类型指向子类对象、重写方法
2.好处：使用父类作为形参可以接收所有的子类对象。
3.弊端：无法使用子类特有的功能。
4.使用强制类型转换。但最好使用instanceof关键字进行类型检查。
5.第一：使用全类名；第二：使用import导入其中一个类，另一个使用全类名。
6。final修饰的特点：
            类：无法被继承；
            方法：无法被重写；
            变量：只能赋值一次。